version: '3.8'

services:
  frontend:
    image: ghcr.io/y-vqv-y/devloom-frontend:latest
    container_name: devloom-frontend
    ports:
      - "80:80"
    restart: unless-stopped
    networks:
      - devloom-network

networks:
  devloom-network:
    driver: bridge
