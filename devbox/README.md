# DevLoom development image

This image is built entirely from this repository and supplies the baseline
Linux toolchain used by the source Taskflow runtime. It contains Git, Python,
Node.js, pnpm, common shell utilities, and native build tools. Project-specific
dependencies remain declared by each generated project.
